#include <iostream>
#include <vector>
#include "error.h"

using namespace std;
/*
string bt(int val, int target, string history) {
    if (val == target) {
        return history
    }
    if (val > target) {
        return 
    }

    return bt(val + 5, target) ||  bt(val * 3, target);
} */


int main() {

    vector<int> v = {};
    cerr << v.back() << '\n';

//    int n; cin >> n;
//    cerr << bt(1, n);
    return 0;
}

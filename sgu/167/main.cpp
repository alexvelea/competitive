#include <iostream>
using namespace std;
int main() {
	cin >> n >> m >> k;
	for (int i = 1; i <= n; ++i)
		for (int j = 1; j <= m; ++j) 
			cin >> el[i][j];
	for (int r = 1; r <= n; ++r) {
		for (int st = 1; st <= n; ++st)
			for (int dr = st; dr <= n; ++dr) 
				for (int d1 = 0; d1 < 2; ++d1)
					for (int d2 = 0; d2 < 2; ++d2) {
								
					}
	}
	return 0;
}

#include <cstdio>

void bt( int lcd, int ind, int x ){
	if( lcd > n )
		return ;
	if( ind == k ){
		int x = n/lcd;
		if( x ) // am ales numar impar de elemente
			rez = rez + 
	}
	// nu iau elementul curent
	bt( lcd, ind+1, x );
	// iau elementul curent
	bt( lcd * El[ind] / gcd( lcd, El[ind] ), ind+1, x^1 );
}


#include <iostream>
#include <map>
using namespace std;
int main() {

/*    
    int n, m; cin >> n >> m;
    map<int, int> viz;
    for (int i = 0; i < n; i += 1) {
        for (int j = 0; j < m; j += 1) {
            int x; cin >> x;
            viz[x] = 1;
        }
    }

    viz.erase(0);

    for (auto& itr : viz) {
        cout << itr.first << ' ';
    }
    cout << '\n';
*/    
}

